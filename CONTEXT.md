# 本地图像处理

此项目是浏览器中的本地图像处理工具。用户文件只在当前浏览器会话中存在，不传至服务器。

## Language

**源图片**：
用户从设备文件系统选择、用于处理的静态图像文件。
_Avoid_: 上传图片、远端图片、动图

**处理会话**：
从选择源图片开始，到页面关闭或刷新为止的本地临时处理范围；不写入 LocalStorage。
_Avoid_: 账户、项目、云端会话、持久化草稿

**导出文件**：
由处理会话生成并下载到用户设备的 PDF 或图片。
_Avoid_: 上传结果、服务器文件

**桌面批量导出**：
桌面浏览器中，将多张源图片按选择顺序各自转换为一个 PDF，或应用同一套水印配置后导出，并合并为一个 ZIP 下载包的导出方式。
_Avoid_: 多图单 PDF

**移动端单图导出**：
移动浏览器中，选择一张源图片并生成一个 PDF 的导出方式。
_Avoid_: 移动端批量导出

**移动端 PDF 交付**：
移动端单图导出后，将 PDF 下载到文件系统或交由系统分享菜单处理的方式。
_Avoid_: 存入相册

**文字水印**：
叠加在源图片上的用户自定义文字；首版唯一支持的水印类型。可配置字号、颜色、不透明度与旋转角度，字体固定为系统无衬线字体。
_Avoid_: 图片水印、Logo 水印

**水印位置**：
文字水印在源图片坐标系中的相对位置。用户可自由拖拽，但旋转后的文字外接范围必须完整位于图片内；批量处理时此相对位置应用到每张图片。
_Avoid_: 固定九宫格位置、越界水印

**重复水印**：
按固定网格密度平铺并覆盖整张源图片的文字水印模式。首版不提供间距控件，但配置保留水平与垂直间距字段。
_Avoid_: 单次水印

**水印图片导出**：
将已叠加文字水印的图像导出为 PNG、JPEG 或 WebP 的能力。
_Avoid_: 仅预览水印、动图导出

**水印 PDF 导出**：
将已叠加文字水印的图像作为内容生成 PDF 的能力；页面与图片比例一致，图片铺满页面且无白边。
_Avoid_: 固定 A4 PDF

**原图 PDF 导出**：
将未经水印处理的源图片转换为 PDF 的能力；页面与图片比例一致，图片铺满页面且无白边。
_Avoid_: 固定 A4 PDF

**支持格式**：
输入为当前浏览器可解码的静态图像格式；水印图片导出固定为 PNG、JPEG、WebP。
_Avoid_: 动图格式

**可处理源图片**：
浏览器可解码、文件不超过 20 MB 且最长边不超过 8,000 px 的静态源图片。
_Avoid_: 超限图片、动图

**支持浏览器**：
桌面 Chrome、Edge 与 iOS、Android 的主流浏览器。Firefox 与旧版浏览器不在兼容承诺内。
_Avoid_: 全浏览器支持

**导出命名**：
导出文件保留源文件基础名并追加功能后缀，例如 `photo-watermarked.png`、`photo.pdf`。同名结果自动追加序号；批量结果按类型打包为 ZIP。
_Avoid_: 无规则随机文件名
